# このファイルを .env にコピーして値を埋めてください
# cp .env.example .env

# ローカル開発はSQLite（そのまま使えます）
# 本番はNeonのPostgreSQLのURLに書き換えます
DATABASE_URL=sqlite:///./jobtrack.db

# フロントエンドの NEXTAUTH_SECRET と必ず同じ値を設定
NEXTAUTH_SECRET=your-secret-here

# カンマ区切りで複数指定可能
CORS_ORIGINS=["http://localhost:3000"]
